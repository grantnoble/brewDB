CREATE TABLE recipes_hops
(
recipes_hops_id mediumint unsigned NOT NULL auto_increment,
recipes_rec_id mediumint unsigned NOT NULL,
hops_rec_id mediumint unsigned NOT NULL,
PRIMARY KEY (recipes_hops_id),
INDEX (recipes_rec_id),
INDEX (hops_rec_id)
);
